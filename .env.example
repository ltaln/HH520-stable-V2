FIRECRAWL_API_KEY=fc-REPLACE_ME
# Optional second Firecrawl account. Used automatically when the primary key quota is exhausted.
FIRECRAWL_API_KEY_BACKUP=fc-REPLACE_ME
# Optional program-side GPT invocation only:
OPENAI_API_KEY=
OPENAI_MODEL=
# ChatGPT Action bridge authentication (not the Firecrawl key):
HH520_ACCESS_TOKEN=
